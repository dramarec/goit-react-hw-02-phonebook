import React, { Component } from "react";
// import styles from "./Button.module.css";

export default class Button extends Component {
    render() {
        return (
            <>
                {/* <button
                    className={styles.button}
                    type="button"
                    // onClick={() => this.props.onItemAdded("Phonebook Number")}
                >
                    Add contact
                </button> */}
            </>
        );
    }
}
